# Implementation Plan: Enforce English-Only Source Code Comments

**Branch**: `feature/wi-5-quellcode-pflegen` | **Date**: 2026-03-08 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/feature/wi-5-quellcode-pflegen/spec.md`

## Summary

Ensure all code comments across the monorepo are written in English. Research confirms the codebase already fully complies — all existing comments are in English. German text exists only in i18n locale strings and configuration content, which are explicitly out of scope. The implementation consists of a verification pass to confirm compliance.

## Technical Context

**Language/Version**: TypeScript 5.x
**Primary Dependencies**: React, React Native, Expo, Vite, Tamagui, TanStack Query
**Storage**: N/A (no data changes)
**Testing**: Manual verification via grep-based scan; `pnpm typecheck` and `pnpm build` for regression
**Target Platform**: Web (Vite + React) and Mobile (Expo + React Native)
**Project Type**: Cross-platform app monorepo
**Performance Goals**: N/A (code maintenance task)
**Constraints**: Must not modify i18n locale strings or configuration content strings
**Scale/Scope**: 187 source files across `apps/` and `packages/`

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

Constitution is not yet configured for this project (template only). No gates to enforce. **PASS** — no violations.

**Post-Phase 1 re-check**: Still passes. No architectural changes introduced by this feature.

## Project Structure

### Documentation (this feature)

```text
specs/feature/wi-5-quellcode-pflegen/
├── plan.md              # This file
├── research.md          # Phase 0 output — confirms no non-English comments exist
├── data-model.md        # Phase 1 output — N/A (no data model changes)
├── quickstart.md        # Phase 1 output — verification guide
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
cp-test-01/
├── apps/
│   ├── web/src/              # Web app source — comments to verify
│   └── mobile/               # Mobile app source — comments to verify
├── packages/
│   ├── ui/src/               # Shared UI components — comments to verify
│   ├── shared/src/           # Shared hooks/providers — comments to verify
│   │   └── i18n/locales/     # i18n files — OUT OF SCOPE (content strings)
│   ├── config/src/           # App configuration — comments to verify
│   └── types/src/            # TypeScript types — comments to verify
```

**Structure Decision**: No structural changes. This is a code maintenance task that only modifies comment text within existing files. The existing monorepo structure (apps/ + packages/) remains unchanged.

## Complexity Tracking

No constitution violations. No complexity justification needed.
